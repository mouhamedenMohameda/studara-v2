# Architecture notes (LLM context)

Short, path-centric summaries of this repository live here so chats can load **small files** instead of re-exploring the whole tree.

**Recommended read order**

1. `overview.md` (when it exists)
2. `repos-and-packages.md`
3. Domain-specific files (`backend.md`, `clients.md`, `data.md`, etc.)

**How to populate or refresh**

Ask Cursor to use the **`project-architecture`** subagent (`.cursor/agents/project-architecture.md`) to analyze the repo and write/update these files.

**Last updated:** not yet captured
